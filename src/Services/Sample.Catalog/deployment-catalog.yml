apiVersion: apps/v1
kind: Deployment
metadata:
  name: catalog
  labels:
    app: eshop
spec:
  replicas: 1
  selector:
    matchLabels:
      service: catalog
  template:
    metadata:
      labels:
        app: eshop
        service: catalog
    spec:
      containers:
        - name: catalog
          image: minoregistry.azurecr.io/microservices/catalog:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 80
              protocol: TCP
          env:
            - name: ASPNETCORE_URLS
              value: http://+:80
---
apiVersion: v1
kind: Service
metadata:
  name: catalog
  labels:
    app: eshop
    service: catalog
spec:
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
  selector:
    service: catalog