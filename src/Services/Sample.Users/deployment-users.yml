apiVersion: apps/v1
kind: Deployment
metadata:
  name: users
  labels:
    app: eshop
spec:
  replicas: 1
  selector:
    matchLabels:
      service: users
  template:
    metadata:
      labels:
        app: eshop
        service: users
    spec:
      containers:
        - name: users
          image: minoregistry.azurecr.io/microservices/users:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 80
              protocol: TCP
          env:
            - name: ASPNETCORE_URLS
              value: http://+:80
---
apiVersion: v1
kind: Service
metadata:
  name: users
  labels:
    app: eshop
    service: users
spec:
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
  selector:
    service: users